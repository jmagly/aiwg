# AIWG Environment Configuration
# Copy to .env and fill in values (never commit .env!)

# ============================================
# npm Publishing (for maintainers)
# ============================================

# npm automation token for publishing
# Generate at: https://www.npmjs.com/settings/<username>/tokens
# Select "Automation" type for CI/CD or "Publish" for local publishing
# For GitHub Actions: Add as repository secret named NPM_TOKEN
NPM_TOKEN=

# ============================================
# Development Options
# ============================================

# Enable debug output
DEBUG=

# Skip update checks during development
AIWG_SKIP_UPDATE_CHECK=

# Override update check interval (milliseconds, default: 86400000 = 24 hours)
AIWG_UPDATE_INTERVAL=

# ============================================
# Channel Configuration
# ============================================

# Force a specific channel (stable or edge)
# Normally managed via: aiwg --use-main / aiwg --use-stable
AIWG_CHANNEL=

# Override edge installation path (default: ~/.local/share/ai-writing-guide)
AIWG_EDGE_PATH=

# Grok Bot user-scope skills root (absolute). Required for --scope user / --global skill deploy.
# AIWG_GROKBOT_SKILLS_DIR=/absolute/path/to/grokbot/skills
